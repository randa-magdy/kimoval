import GalleryCarousel from '@/components/UI/GalleryCarousel';
import { FC } from 'react';

const DeviceSlider: FC = () => {
  return (
    <div className="device-slider w-full">
      <GalleryCarousel />
    </div>
  );
};

export default DeviceSlider;
